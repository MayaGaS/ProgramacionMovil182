// expresion ? 'si es true' : 'si es false'
// el operador ternario cumple con una funcion muy similar al operador de control de flujo if

let edad = 25;
let acceso = edad > 17 ? 'Permitir ingreso' : 'No puede ingresar';

console.log(acceso);